;; if we treat the if as a normal function, the application order will
;; make it first evaluate all the parmeter passed into the funciton
;; so it will try to evaluate sqrt-iter and so infinite loop.
;;
;;
